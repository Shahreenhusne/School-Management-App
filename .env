

DATABASE_URL="postgresql://shahreen:school1234@localhost:5432/school_management_app?schema=public"

# "postgres://USERNAME:PASSWORD@localhost:PORT/DB NAME"
# user = a valid Postgres user inside your Docker container
# password = the correct password for that user
# db = the database actually exists